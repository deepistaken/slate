/**
 * DEPRECATED — Slate no longer uses the Lovable AI gateway.
 *
 * Model access now goes through Google's Gemini API directly. See
 * `src/lib/ai-provider.ts` (`getModel` / `MODELS`). This file is intentionally
 * left as a stub with no imports so the old Lovable dependency can be removed;
 * it can be deleted once you've confirmed nothing references it.
 */
export {};
